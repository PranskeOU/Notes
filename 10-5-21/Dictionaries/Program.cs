using System;

namespace Dictionaries
{
    class Program
    {
        static void Main(string[] args)
        {
            //Works as a Key Value Pair
            //Provide the key, you get the value
            //Similar to a list, but easy lookup with the key
            //Dictionary<keyBasetype, valueBaseType><variableName>;
            //myDictionary.Add(key, value);
        }
    }
}
