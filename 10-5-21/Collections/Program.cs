using System;

namespace Collections
{
    class Program
    {
        static void Main(string[] args)
        {
            //Store several values, all in one variable
            //We access each value by using the index or "key"
            //An array starts counting at 0 (0, 1, 2, 3)
            //You have to set the size manually (must know how many elements)
            //String is an array of type "char"
            //char[] myString
            //"Length" property
            //<baseType>[] <variableName>;
            int[] myIntArray = { 5, 9, 10, 2, 99 };
            int[] myIntArray1 = new int[5];

            //myArray[INDEX] = value
                //assigns a value to a particular index
        }
    }
}
