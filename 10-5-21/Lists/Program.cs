using System;

namespace Lists
{
    class Program
    {
        static void Main(string[] args)
        {
            //Different than an ArrayList, but does not hold a generic object
            //Lists have a specific data type
            //Does not have a specified size like arrays
            //"count" function
                //Counts the number of elements in the list(used for iterations)
            //List<baseType><variableName>

        }
    }
}
